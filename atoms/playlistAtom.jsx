import { atom } from "recoil";
export const playlistState = atom({
  key: "playlistState",
  default: null,
});

export const playlistIdState = atom({
  key: "playlistIdState",
  default: null,
});

export const featuredlistState = atom({
  key: "featuredlistState",
  default: null,
});

export const featuredlistIdState = atom({
  key: "featuredlistIdState",
  default: null,
});
