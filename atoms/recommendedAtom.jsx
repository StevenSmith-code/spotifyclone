import { atom } from "recoil";
export const recommendState = atom({
  key: "recommendState",
  default: null,
});

export const recommendIdState = atom({
  key: "recommendIdState",
  default: null,
});
